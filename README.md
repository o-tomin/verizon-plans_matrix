# verizon-plans_matrix
This repository hosts the configuration file (`plans_matrix.yml`) that defines **feature accessibility rules** across different **Plans** (Free, Basic, Premium) and **User Roles** (Parent, Child, Member).  
